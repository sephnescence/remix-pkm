import { receptionLoader } from '~/controllers/ReceptionController'

export const loader = receptionLoader

export default function ReceptionRoute() {
  return <div>Loading...</div> // Not that this should be shown
}
